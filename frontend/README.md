# Pivi Social App - Frontend

React-based frontend for the Pivi social platform.

## Running Locally

```bash
npm install
npm start
```

The app will open at `http://localhost:3000`

## Building for Production

```bash
npm run build
```

This creates an optimized production build in the `build/` folder.
